export type Book = {
  id: string;
  title: string;
  author: string;
  createdAt: Date;
  updatedAt: Date;
  description: string;
  publishDate: string;
  likes: number;
  pages: number;
};

// TODO: Possible fields (comments)
